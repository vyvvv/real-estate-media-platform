export type CaseContact = {
  contactId: number;
  firstName: string;
  lastName: string;
  companyName: string;
  profileUrl: string | null;
  email: string;
  phoneNumber: string;
  listingCaseId: number;
};